/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // Turbopack options usually not needed for stable Next 14 build, removing to avoid issues
};

export default nextConfig;
